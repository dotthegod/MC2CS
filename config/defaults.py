"""Default configuration values for the converter."""

# Default scale: 1 Minecraft block = this many Hammer units
DEFAULT_BLOCK_SCALE = 64

# Default CS2 material when no texture pack is loaded
DEFAULT_MATERIAL = "materials/dev/reflectivity_30.vmat"

# Default texture scale for vmap face data
DEFAULT_TEXTURE_SCALE = 0.125

# VMap editor metadata
EDITOR_BUILD = 10553
EDITOR_VERSION = 400

# Default sky
DEFAULT_SKY = "sky_day01_01"

# Smoothing angle for meshes
DEFAULT_SMOOTHING_ANGLE = 40.0

# Chunk size for spatial chunking output mode (in blocks)
DEFAULT_CHUNK_SIZE = 16
